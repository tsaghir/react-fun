import { css } from 'emotion';

const navStyle = css({
  padding: '20px 0',
  marginBottom: '20px',
  width: '100%',
  height: 'auto',
  boxShadow: '0 4px 8px 0 rgba(0, 0, 0, 0.2)',
});

const navTitleStyle = css({
  fontFamily: 'Roboto thin',
  paddingLeft: 30,
  fontSize: 30,
});

export { navStyle, navTitleStyle };
